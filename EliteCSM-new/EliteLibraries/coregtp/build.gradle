description = 'coregtp'

def depPojects = ["elitecommons"]
dependencies {
    depPojects.each {
        compile project(":" + it)
        testCompile files(project(":" + it).sourceSets.test.output)
    }
}