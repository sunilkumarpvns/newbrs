package com.elitecore.core.imdg.autogen;

//
// Generated by mibgen version 5.1 (03/08/07) when compiling IMDG-MIB in standard metadata mode.
//

// java imports
//
import java.io.Serializable;
import java.util.Hashtable;

// jmx imports
//
import javax.management.MBeanServer;
import javax.management.ObjectName;
import javax.management.InstanceAlreadyExistsException;

// jdmk imports
//
import com.sun.management.snmp.agent.SnmpMib;
import com.sun.management.snmp.agent.SnmpMibNode;
import com.sun.management.snmp.agent.SnmpMibTable;
import com.sun.management.snmp.agent.SnmpStandardObjectServer;

/**
 * The class is used for representing "IMDG-MIB".
 * You can edit the file if you want to modify the behavior of the MIB.
 */
public class IMDG_MIB extends SnmpMib implements Serializable {

    /**
     * Default constructor. Initialize the Mib tree.
     */
    public IMDG_MIB() {
        mibName = "IMDG_MIB";
    }

    /**
     * Initialization of the MIB with no registration in Java DMK.
     */
    public void init() throws IllegalAccessException {
        // Allow only one initialization of the MIB.
        //
        if (isInitialized == true) {
            return ;
        }

        try  {
            populate(null, null);
        } catch(IllegalAccessException x)  {
            throw x;
        } catch(RuntimeException x)  {
            throw x;
        } catch(Exception x)  {
            throw new Error(x.getMessage());
        }

        isInitialized = true;
    }

    /**
     * Initialization of the MIB with AUTOMATIC REGISTRATION in Java DMK.
     */
    public ObjectName preRegister(MBeanServer server, ObjectName name)
            throws Exception {
        // Allow only one initialization of the MIB.
        //
        if (isInitialized == true) {
            throw new InstanceAlreadyExistsException();
        }

        // Initialize MBeanServer information.
        //
        this.server = server;

        populate(server, name);

        isInitialized = true;
        return name;
    }

    /**
     * Initialization of the MIB with no registration in Java DMK.
     */
    public void populate(MBeanServer server, ObjectName name) 
        throws Exception {
        // Allow only one initialization of the MIB.
        //
        if (isInitialized == true) {
            return ;
        }

        if (objectserver == null) 
            objectserver = new SnmpStandardObjectServer();

        // Initialization of the "Imdg" group.
        // To disable support of this group, redefine the 
        // "createImdgMetaNode()" factory method, and make it return "null"
        //
        initImdg(server);

        // Initialization of the "AlertInfoGroup" group.
        // To disable support of this group, redefine the 
        // "createAlertInfoGroupMetaNode()" factory method, and make it return "null"
        //
        initAlertInfoGroup(server);

        // Initialization of the "Members" group.
        // To disable support of this group, redefine the 
        // "createMembersMetaNode()" factory method, and make it return "null"
        //
        initMembers(server);

        // Initialization of the "MapStatistics" group.
        // To disable support of this group, redefine the 
        // "createMapStatisticsMetaNode()" factory method, and make it return "null"
        //
        initMapStatistics(server);

        isInitialized = true;
    }


    // ------------------------------------------------------------
    // 
    // Initialization of the "Imdg" group.
    // 
    // ------------------------------------------------------------


    /**
     * Initialization of the "Imdg" group.
     * 
     * To disable support of this group, redefine the 
     * "createImdgMetaNode()" factory method, and make it return "null"
     * 
     * @param server    MBeanServer for this group (may be null)
     * 
     **/
    protected void initImdg(MBeanServer server) 
        throws Exception {
        final String oid = getGroupOid("Imdg", "1.3.6.1.4.1.21067.6");
        ObjectName objname = null;
        if (server != null) {
            objname = getGroupObjectName("Imdg", oid, mibName + ":name=com.elitecore.core.imdg.autogen.Imdg");
        }
        final ImdgMeta meta = createImdgMetaNode("Imdg", oid, objname, server);
        if (meta != null) {
            meta.registerTableNodes( this, server );

            // Note that when using standard metadata,
            // the returned object must implement the "ImdgMBean"
            // interface.
            //
            final ImdgMBean group = (ImdgMBean) createImdgMBean("Imdg", oid, objname, server);
            meta.setInstance( group );
            registerGroupNode("Imdg", oid, objname, meta, group, server);
        }
    }


    /**
     * Factory method for "Imdg" group metadata class.
     * 
     * You can redefine this method if you need to replace the default
     * generated metadata class with your own customized class.
     * 
     * @param groupName Name of the group ("Imdg")
     * @param groupOid  OID of this group
     * @param groupObjname ObjectName for this group (may be null)
     * @param server    MBeanServer for this group (may be null)
     * 
     * @return An instance of the metadata class generated for the
     *         "Imdg" group (ImdgMeta)
     * 
     **/
    protected ImdgMeta createImdgMetaNode(String groupName,
                String groupOid, ObjectName groupObjname, MBeanServer server)  {
        return new ImdgMeta(this, objectserver);
    }


    /**
     * Factory method for "Imdg" group MBean.
     * 
     * You can redefine this method if you need to replace the default
     * generated MBean class with your own customized class.
     * 
     * @param groupName Name of the group ("Imdg")
     * @param groupOid  OID of this group
     * @param groupObjname ObjectName for this group (may be null)
     * @param server    MBeanServer for this group (may be null)
     * 
     * @return An instance of the MBean class generated for the
     *         "Imdg" group (Imdg)
     * 
     * Note that when using standard metadata,
     * the returned object must implement the "ImdgMBean"
     * interface.
     **/
    protected Object createImdgMBean(String groupName,
                String groupOid,  ObjectName groupObjname, MBeanServer server)  {

        // Note that when using standard metadata,
        // the returned object must implement the "ImdgMBean"
        // interface.
        //
        if (server != null) 
            return new Imdg(this,server);
        else 
            return new Imdg(this);
    }


    // ------------------------------------------------------------
    // 
    // Initialization of the "AlertInfoGroup" group.
    // 
    // ------------------------------------------------------------


    /**
     * Initialization of the "AlertInfoGroup" group.
     * 
     * To disable support of this group, redefine the 
     * "createAlertInfoGroupMetaNode()" factory method, and make it return "null"
     * 
     * @param server    MBeanServer for this group (may be null)
     * 
     **/
    protected void initAlertInfoGroup(MBeanServer server) 
        throws Exception {
        final String oid = getGroupOid("AlertInfoGroup", "1.3.6.1.4.1.21067.6.2");
        ObjectName objname = null;
        if (server != null) {
            objname = getGroupObjectName("AlertInfoGroup", oid, mibName + "_Imdg:name=com.elitecore.core.imdg.autogen.AlertInfoGroup");
        }
        final AlertInfoGroupMeta meta = createAlertInfoGroupMetaNode("AlertInfoGroup", oid, objname, server);
        if (meta != null) {
            meta.registerTableNodes( this, server );

            // Note that when using standard metadata,
            // the returned object must implement the "AlertInfoGroupMBean"
            // interface.
            //
            final AlertInfoGroupMBean group = (AlertInfoGroupMBean) createAlertInfoGroupMBean("AlertInfoGroup", oid, objname, server);
            meta.setInstance( group );
            registerGroupNode("AlertInfoGroup", oid, objname, meta, group, server);
        }
    }


    /**
     * Factory method for "AlertInfoGroup" group metadata class.
     * 
     * You can redefine this method if you need to replace the default
     * generated metadata class with your own customized class.
     * 
     * @param groupName Name of the group ("AlertInfoGroup")
     * @param groupOid  OID of this group
     * @param groupObjname ObjectName for this group (may be null)
     * @param server    MBeanServer for this group (may be null)
     * 
     * @return An instance of the metadata class generated for the
     *         "AlertInfoGroup" group (AlertInfoGroupMeta)
     * 
     **/
    protected AlertInfoGroupMeta createAlertInfoGroupMetaNode(String groupName,
                String groupOid, ObjectName groupObjname, MBeanServer server)  {
        return new AlertInfoGroupMeta(this, objectserver);
    }


    /**
     * Factory method for "AlertInfoGroup" group MBean.
     * 
     * You can redefine this method if you need to replace the default
     * generated MBean class with your own customized class.
     * 
     * @param groupName Name of the group ("AlertInfoGroup")
     * @param groupOid  OID of this group
     * @param groupObjname ObjectName for this group (may be null)
     * @param server    MBeanServer for this group (may be null)
     * 
     * @return An instance of the MBean class generated for the
     *         "AlertInfoGroup" group (AlertInfoGroup)
     * 
     * Note that when using standard metadata,
     * the returned object must implement the "AlertInfoGroupMBean"
     * interface.
     **/
    protected Object createAlertInfoGroupMBean(String groupName,
                String groupOid,  ObjectName groupObjname, MBeanServer server)  {

        // Note that when using standard metadata,
        // the returned object must implement the "AlertInfoGroupMBean"
        // interface.
        //
        if (server != null) 
            return new AlertInfoGroup(this,server);
        else 
            return new AlertInfoGroup(this);
    }


    // ------------------------------------------------------------
    // 
    // Initialization of the "Members" group.
    // 
    // ------------------------------------------------------------


    /**
     * Initialization of the "Members" group.
     * 
     * To disable support of this group, redefine the 
     * "createMembersMetaNode()" factory method, and make it return "null"
     * 
     * @param server    MBeanServer for this group (may be null)
     * 
     **/
    protected void initMembers(MBeanServer server) 
        throws Exception {
        final String oid = getGroupOid("Members", "1.3.6.1.4.1.21067.6.11");
        ObjectName objname = null;
        if (server != null) {
            objname = getGroupObjectName("Members", oid, mibName + "_Imdg:name=com.elitecore.core.imdg.autogen.Members");
        }
        final MembersMeta meta = createMembersMetaNode("Members", oid, objname, server);
        if (meta != null) {
            meta.registerTableNodes( this, server );

            // Note that when using standard metadata,
            // the returned object must implement the "MembersMBean"
            // interface.
            //
            final MembersMBean group = (MembersMBean) createMembersMBean("Members", oid, objname, server);
            meta.setInstance( group );
            registerGroupNode("Members", oid, objname, meta, group, server);
        }
    }


    /**
     * Factory method for "Members" group metadata class.
     * 
     * You can redefine this method if you need to replace the default
     * generated metadata class with your own customized class.
     * 
     * @param groupName Name of the group ("Members")
     * @param groupOid  OID of this group
     * @param groupObjname ObjectName for this group (may be null)
     * @param server    MBeanServer for this group (may be null)
     * 
     * @return An instance of the metadata class generated for the
     *         "Members" group (MembersMeta)
     * 
     **/
    protected MembersMeta createMembersMetaNode(String groupName,
                String groupOid, ObjectName groupObjname, MBeanServer server)  {
        return new MembersMeta(this, objectserver);
    }


    /**
     * Factory method for "Members" group MBean.
     * 
     * You can redefine this method if you need to replace the default
     * generated MBean class with your own customized class.
     * 
     * @param groupName Name of the group ("Members")
     * @param groupOid  OID of this group
     * @param groupObjname ObjectName for this group (may be null)
     * @param server    MBeanServer for this group (may be null)
     * 
     * @return An instance of the MBean class generated for the
     *         "Members" group (Members)
     * 
     * Note that when using standard metadata,
     * the returned object must implement the "MembersMBean"
     * interface.
     **/
    protected Object createMembersMBean(String groupName,
                String groupOid,  ObjectName groupObjname, MBeanServer server)  {

        // Note that when using standard metadata,
        // the returned object must implement the "MembersMBean"
        // interface.
        //
        if (server != null) 
            return new Members(this,server);
        else 
            return new Members(this);
    }


    // ------------------------------------------------------------
    // 
    // Initialization of the "MapStatistics" group.
    // 
    // ------------------------------------------------------------


    /**
     * Initialization of the "MapStatistics" group.
     * 
     * To disable support of this group, redefine the 
     * "createMapStatisticsMetaNode()" factory method, and make it return "null"
     * 
     * @param server    MBeanServer for this group (may be null)
     * 
     **/
    protected void initMapStatistics(MBeanServer server) 
        throws Exception {
        final String oid = getGroupOid("MapStatistics", "1.3.6.1.4.1.21067.6.10");
        ObjectName objname = null;
        if (server != null) {
            objname = getGroupObjectName("MapStatistics", oid, mibName + "_Imdg:name=com.elitecore.core.imdg.autogen.MapStatistics");
        }
        final MapStatisticsMeta meta = createMapStatisticsMetaNode("MapStatistics", oid, objname, server);
        if (meta != null) {
            meta.registerTableNodes( this, server );

            // Note that when using standard metadata,
            // the returned object must implement the "MapStatisticsMBean"
            // interface.
            //
            final MapStatisticsMBean group = (MapStatisticsMBean) createMapStatisticsMBean("MapStatistics", oid, objname, server);
            meta.setInstance( group );
            registerGroupNode("MapStatistics", oid, objname, meta, group, server);
        }
    }


    /**
     * Factory method for "MapStatistics" group metadata class.
     * 
     * You can redefine this method if you need to replace the default
     * generated metadata class with your own customized class.
     * 
     * @param groupName Name of the group ("MapStatistics")
     * @param groupOid  OID of this group
     * @param groupObjname ObjectName for this group (may be null)
     * @param server    MBeanServer for this group (may be null)
     * 
     * @return An instance of the metadata class generated for the
     *         "MapStatistics" group (MapStatisticsMeta)
     * 
     **/
    protected MapStatisticsMeta createMapStatisticsMetaNode(String groupName,
                String groupOid, ObjectName groupObjname, MBeanServer server)  {
        return new MapStatisticsMeta(this, objectserver);
    }


    /**
     * Factory method for "MapStatistics" group MBean.
     * 
     * You can redefine this method if you need to replace the default
     * generated MBean class with your own customized class.
     * 
     * @param groupName Name of the group ("MapStatistics")
     * @param groupOid  OID of this group
     * @param groupObjname ObjectName for this group (may be null)
     * @param server    MBeanServer for this group (may be null)
     * 
     * @return An instance of the MBean class generated for the
     *         "MapStatistics" group (MapStatistics)
     * 
     * Note that when using standard metadata,
     * the returned object must implement the "MapStatisticsMBean"
     * interface.
     **/
    protected Object createMapStatisticsMBean(String groupName,
                String groupOid,  ObjectName groupObjname, MBeanServer server)  {

        // Note that when using standard metadata,
        // the returned object must implement the "MapStatisticsMBean"
        // interface.
        //
        if (server != null) 
            return new MapStatistics(this,server);
        else 
            return new MapStatistics(this);
    }


    // ------------------------------------------------------------
    // 
    // Implements the "registerTableMeta" method defined in "SnmpMib".
    // See the "SnmpMib" Javadoc API for more details.
    // 
    // ------------------------------------------------------------

    public void registerTableMeta( String name, SnmpMibTable meta) {
        if (metadatas == null) return;
        if (name == null) return;
        metadatas.put(name,meta);
    }


    // ------------------------------------------------------------
    // 
    // Implements the "getRegisteredTableMeta" method defined in "SnmpMib".
    // See the "SnmpMib" Javadoc API for more details.
    // 
    // ------------------------------------------------------------

    public SnmpMibTable getRegisteredTableMeta( String name ) {
        if (metadatas == null) return null;
        if (name == null) return null;
        return (SnmpMibTable) metadatas.get(name);
    }

    public SnmpStandardObjectServer getStandardObjectServer() {
        if (objectserver == null) 
            objectserver = new SnmpStandardObjectServer();
        return objectserver;
    }

    private boolean isInitialized = false;

    protected SnmpStandardObjectServer objectserver;

    protected final Hashtable metadatas = new Hashtable();
}
