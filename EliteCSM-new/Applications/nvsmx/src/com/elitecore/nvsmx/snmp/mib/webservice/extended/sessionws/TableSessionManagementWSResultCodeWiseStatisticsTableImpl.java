package com.elitecore.nvsmx.snmp.mib.webservice.extended.sessionws;


//
// Generated by mibgen version 5.1 (03/08/07) when compiling NETVERTEX-PCRF-MIB.
//

// java imports
//
import java.io.Serializable;

import javax.management.MBeanServer;
import javax.management.ObjectName;

import com.elitecore.commons.logging.LogManager;
import com.elitecore.nvsmx.snmp.mib.webservice.autogencode.SessionWSResultCodeWiseEntryMBean;
import com.elitecore.nvsmx.snmp.mib.webservice.autogencode.TableSessionManagementWSResultCodeWiseStatisticsTable;
import com.sun.management.snmp.SnmpStatusException;
import com.sun.management.snmp.agent.SnmpMib;

 
public class TableSessionManagementWSResultCodeWiseStatisticsTableImpl extends TableSessionManagementWSResultCodeWiseStatisticsTable implements Serializable {

	private static final long serialVersionUID = 1L;
	private static final String MODULE = "SESSION-WS-CODE-WISE-STATS-TABLE";

	public TableSessionManagementWSResultCodeWiseStatisticsTableImpl(SnmpMib myMib, MBeanServer server) {
		super(myMib, server);
	}
	
	@Override
	public synchronized void addEntry(SessionWSResultCodeWiseEntryMBean entry, ObjectName name) throws SnmpStatusException {
		super.addEntry(entry, name);
		
		if(server != null){
			try {
				server.registerMBean(entry, name);
			} catch (Exception e) {
				LogManager.getLogger().error(MODULE, "SessionWS ResultCode wise statistics entry for ( " +entry.getSessionWSResultCodeName()+","+entry.getSessionWSResultCode()+" ) " +
						" not added in TableSessionManagementWSResultCodeWiseStatisticsTable. Reason: " + e.getMessage());
				LogManager.getLogger().trace(MODULE, e);
			}
		}
	}

}
