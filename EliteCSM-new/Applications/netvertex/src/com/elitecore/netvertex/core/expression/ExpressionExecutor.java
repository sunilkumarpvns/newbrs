package com.elitecore.netvertex.core.expression;

/**
 * An {@link ExpressionExecutor} that executes each submitted expression
 * and returns the results
 *
 * @since NetVertex 6.0
 * @author Subhash Punani
 */
public interface ExpressionExecutor {
	
	boolean execute();
}
